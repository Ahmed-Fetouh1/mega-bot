# ── FOR HUGGING FACE SPACES (and other Linux hosts) ──────────────────────────
# tgcrypto builds fine on Linux — keep it for faster uploads/downloads.
#
# Install: pip install -r requirements.txt

pyrogram==2.0.106
tgcrypto==1.2.5
mega.py==1.0.8
python-dotenv==1.0.1
gradio==4.44.0
