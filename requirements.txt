# Telegram MTProto client — supports files up to 2 GB
pyrogram==2.0.106

# Required C-extension for Pyrogram's crypto (much faster uploads/downloads)
tgcrypto==1.2.5

# Mega.nz download library
mega.py==1.0.8

# Load environment variables from a .env file during local development
python-dotenv==1.0.1
